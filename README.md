# ⚡ My k3s cluster configurations files

In this repository you will find all the necessary files to create a k3s cluster.

- `ansible`: ansible configuration
- `k3s`: k3s configuration
- `images`: docker images
